use sysio as io;
use typing;

// This is a simple example for the Entropia Language types

fn main() {
	let a: int = 1;
	let b: float = 2.0;
	let c: byte = 3;
	let d: char = 'd';
	let e: bool = true;
	let f: str = "Hello World!";
	let g: auto = 1;
	io::println(typing::typeof(g));
}